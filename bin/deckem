#!/usr/bin/env node

var path = require('path');

require('scaffolder').create(path.resolve(__dirname, '../'), function(builder, action) {
	(action || builder.build).call(builder, function() {
		// done
	});
});